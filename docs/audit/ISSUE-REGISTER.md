# ISSUE-REGISTER.md — Sổ Theo Dõi Phát Hiện Lỗ Hổng & Sai Lệch

- **Dự án**: Nền tảng Cho thuê QNS
- **Căn cứ**: Mục 5 và Mục 12.1 trong `Ban-thu-hoach-va-chi-thi-AI-agent-batdongsan.md` (19/09/2026)
- **Chuẩn định dạng**: `ID | Priority | Evidence type | File/function | Reproduction | Expected | Actual | Root cause | Fix commit | Regression test | Status | Remaining risk`
- **Trạng thái hợp lệ**: `discovered → reproduced/confirmed-by-code → fixing → verified → closed` (hoặc `blocked`)

---

## Bảng Chi Tiết Toàn Bộ Phát Hiện (Hợp Nhất F01–F16 & Danh Mục Kiểm Toán)

| ID | Priority | Evidence type | File/function | Reproduction | Expected | Actual | Root cause | Fix commit | Regression test | Status | Remaining risk |
|---|---|---|---|---|---|---|---|---|---|---|---|
| **F01** (=OPS-08) | P0 | confirmed-by-code | `apps/web/package.json` | Khóa `next: 14.2.15`, kiểm tra CVE-2025-RSC-DoS | Không chứa CVE DoS nghiêm trọng đã công bố | Next.js 14.2.15 nằm trong dải bị ảnh hưởng DoS RSC | Bản 14.x cũ chưa cập nhật patch security mới nhất | pending | SCA npm audit + build 31 routes | confirmed-by-code | Cần kiểm tra kỹ breaking changes trước khi nâng lên Next.js 15 |
| **F02** (=FIN-01) | P0 | confirmed-by-code | `apps/api/src/modules/membership/membership.service.ts#approveRequest` | Gọi POST `/admin/membership-requests/:id/approve` không gửi `externalTransactionId` hoặc `confirmedAmount` | Bắt buộc DTO có mã ngân hàng thật và số tiền nguyên dương | Service tự sinh `BANK_MANUAL_...` và lấy `quotedAmount` nếu thiếu | Thiếu DTO validation ràng buộc dữ liệu đầu vào | pending | POST invalid body -> 400 Bad Request; race 2 requests -> 1 ledger | confirmed-by-code | Nhập sai mã ngân hàng từ phía nhân viên vận hành thủ công |
| **F03** (=FIN-01) | P0 | confirmed-by-code | `apps/api/src/modules/membership/membership.service.ts#refundRequest` | Gửi hoàn tiền với `refundAmount` âm, số 0 hoặc lớn hơn số tiền đã thu | Chặn và báo lỗi validation, số hoàn <= tiền đã thu | Cho phép nhập số bất kỳ, ép kiểu BigInt trực tiếp | Thiếu DTO kiểm tra trần số tiền hoàn và số dương | pending | Hoàn vượt mức -> 400 Bad Request; hoàn 2 lần -> từ chối | confirmed-by-code | Thao tác hoàn tiền ngoài ngân hàng chưa tự động đối soát qua webhook |
| **F04** (=FIN-04) | P1 | confirmed-by-code | `apps/api/src/modules/listings/listings.service.ts#create` & `membership.service.ts` | Tạo tin đọc `activeMembership.plan.maxActiveListings` trong khi mua gói lưu snapshot | Sử dụng `planSnapshot` bất biến đã mua ở mọi nơi | Chỗ đọc snapshot, chỗ đọc catalog trực tiếp | Không có một service tập trung trả về Entitlement hiệu lực | pending | Đổi giá catalog -> gói cũ giữ nguyên quota và thời hạn | confirmed-by-code | Dữ liệu cũ trước khi có snapshot cần script backfill |
| **F05** (=RB-05) | P1 | confirmed-by-code | `apps/api/src/modules/listings/listings.service.ts#create` & `admin.service.ts#approveListing` | Gửi 20 request tạo tin hoặc duyệt tin đồng thời khi quota chỉ còn 1 | Chỉ duy nhất 1 request thành công, các request khác báo hết hạn mức | Race condition vượt quota do count và create/update tách rời | Thiếu atomic transaction hoặc khóa slot cấp user | pending | Parallel 20 create requests -> max quota không bị vượt | confirmed-by-code | Độ trễ tăng nhẹ khi thực thi transaction tuần tự |
| **F06** (=RB-02) | P1 | confirmed-by-code | `apps/api/src/modules/auth/otp.service.ts` | Gửi OTP từ instance A, gửi xác thực tới instance B | OTP được chia sẻ qua Redis tập trung, xác thực thành công | Lỗi vì OTP lưu trong in-memory `Map` cục bộ; sinh số bằng `Math.random` | Bộ nhớ tạm process-level, chưa dùng Redis và CSPRNG | pending | Đa instance verify OTP; restart node không mất mã; crypto.randomInt | confirmed-by-code | Redis connection timeout cần fallback an toàn |
| **F07** (=RB-01) | P1 | confirmed-by-code | `apps/api/src/modules/auth/strategies/jwt.strategy.ts` & `auth.service.ts` | Đổi mật khẩu/khóa tài khoản, dùng lại token cũ | Token cũ bị từ chối 401 Unauthorized ngay lập tức | Token cũ vẫn hợp lệ nếu payload không có trường tokenVersion | Strategy bỏ qua kiểm tra version khi claim bị thiếu | pending | Đổi mật khẩu -> token cũ nhận 401; logout -> token cũ nhận 401 | confirmed-by-code | Khách hàng phải đăng nhập lại trên các thiết bị khác |
| **F08** (=RB-04) | P1 | confirmed-by-code | `apps/api/src/modules/auth/dto/bootstrap-admin.dto.ts` | Regex SĐT `^0[3\|5\|7\|8\|9]` nhận nhầm ký tự pipe `\|` | Chỉ nhận SĐT 10 chữ số chuẩn mạng viễn thông Việt Nam | Nhận cả chuỗi chứa ký tự thẳng `\|` | Lỗi cú pháp biểu thức chính quy (character class) | pending | Gửi SĐT chứa `\|` -> 400 Bad Request; SĐT hợp lệ -> 200 | confirmed-by-code | Cần khóa hoàn toàn HTTP bootstrap ở production |
| **F09** (=RB-06) | P1 | confirmed-by-code | `apps/api/src/modules/outbox/outbox.service.ts` & mutation services | Tạo tin, duyệt tin, tạo lead nhưng process bị kill đột ngột | Sự kiện được lưu trong outbox cùng DB transaction, worker xử lý sau | Bị mất thông báo hoặc gửi email trực tiếp ngoài transaction | Outbox chưa được gắn vào toàn bộ domain mutations | pending | Commit DB -> event có trong outbox; worker crash -> lease reclaim | confirmed-by-code | Email nhà mạng có thể gửi trễ vài giây so với thời gian thực |
| **F10** (=RB-11) | P1 | confirmed-by-code | `apps/api/src/modules/leads/leads.service.ts#createLead` | Gửi lead vào tin đã hết hạn (`expiresAt < now`) hoặc chủ tin bị khóa | Báo lỗi tin không khả dụng, từ chối nhận lead mới | Vẫn tạo lead thành công vì chỉ kiểm tra `status === 'active'` | Không tái sử dụng `getPublicWhereClause` dùng chung | pending | Lead vào tin hết hạn -> 400 Bad Request; lead vào chủ block -> 400 | confirmed-by-code | Khách thuê không liên hệ được phòng đã hết hạn |
| **F11** (=FE-N17) | P1 | confirmed-by-code | `apps/web/src/components/MoveInCostEstimator.tsx` | Nhập `depositAmount = 0` (miễn cọc) | Công cụ nhận diện đúng cọc = 0 đ, không tự gán cọc 1 tháng | Bị rơi vào fallback tự gán cọc 1 tháng do kiểm tra truthy | Sai lệch kiểu dữ liệu giữa số 0 và falsy | pending | Nhập 0 -> hiển thị 0 đ; không khai báo -> hiển thị "Chưa khai báo" | confirmed-by-code | Cần chủ phòng nhập đầy đủ dữ liệu để tính toán chính xác nhất |
| **F12** (NEW-RBAC)| P1 | confirmed-by-code | `apps/api/src/modules/admin/` & `packages/database/prisma/schema.prisma` | Kiểm duyệt viên truy cập module hoàn tiền tài chính | Bị chặn 403 Forbidden do không có capability tài chính | Truy cập được vì toàn bộ hệ thống dùng chung 1 role `admin` | Thiếu mô hình phân quyền theo capability | pending | Moderator không thể approve refund; Finance không thể toggle block user | confirmed-by-code | Cần cấu hình phân quyền ban đầu cho nhân sự |
| **F13** (=FIN-08) | P1 | confirmed-by-code | `packages/database/prisma/schema.prisma` (`FinanceLedger`) | Xóa tài khoản User có liên kết sổ cái | Cơ sở dữ liệu chặn thao tác xóa để bảo toàn sổ cái vĩnh viễn | Bị xóa cascade nếu không đặt quy tắc bảo vệ | Khóa ngoại thiếu ràng buộc `onDelete: Restrict` | pending | Thử xóa user có ledger -> DB ném lỗi Foreign Key Restrict | confirmed-by-code | User muốn xóa tài khoản chỉ được phép soft-delete |
| **F14** (=RB-09) | P1 | confirmed-by-code | `apps/web/next.config.mjs` | Tải ảnh từ domain bất kỳ qua Next.js Image Optimization | Chỉ cho phép tải ảnh từ CDN và domain trong allowlist | Chấp nhận toàn bộ domain do wildcard `hostname: '**'` | Cấu hình remotePatterns quá mở | pending | Tải ảnh từ domain ngoài -> Next.js chặn; domain hợp lệ -> tối ưu WebP | confirmed-by-code | Cần khai báo domain mới khi đổi nhà cung cấp lưu trữ |
| **F15** (=BR-01) | P2 | confirmed-by-code | `apps/web/src/app/` & `components/` | Tìm kiếm cụm từ "chính chủ" trong UI copy | Không có bất kỳ cam kết "100% chính chủ" nào trên giao diện | Xuất hiện ở 7 tệp metadata, card và trang tĩnh | Chưa rà soát triệt để từ ngữ cấm sau khi pivot | pending | Grep toàn bộ repo -> 0 kết quả cho các từ cấm | confirmed-by-code | Cần nhắc nhở chủ phòng không đặt tiêu đề chứa từ cấm |
| **F16** (=FE-N22) | P2 | confirmed-by-code | `apps/web/src/app/page.tsx` | Một API nhóm tin bị lỗi mạng (500/timeout) | Các nhóm tin khác vẫn hiển thị bình thường; nhóm lỗi hiện thông báo | Toàn bộ 4 nhóm tin biến thành rỗng hoặc trang chủ báo "Chưa có tin" | Dùng `Promise.all` khiến lỗi 1 nhóm làm hỏng kết quả chung | pending | Giả lập 1 nhóm 500 -> 3 nhóm còn lại render bình thường | confirmed-by-code | Server Next.js cần log chi tiết lỗi nhóm bị gián đoạn |
| **FE-N01** | P0 | confirmed-by-code | `apps/web/src/components/ContactBrokerModal.tsx` | Render component và mở modal sau lần render đầu | Hooks chạy ổn định, modal mở mượt mà không lỗi React | Nguy cơ crash React do early return `if (!isOpen) return null;` trước hooks | Vi phạm Rules of Hooks của React | pending | Mở/đóng modal 5 lần liên tiếp trên desktop và mobile | confirmed-by-code | Không có |
| **FE-N02** | P0 | confirmed-by-code | `apps/web/src/app/gia-thanh-vien/page.tsx` | Chạy ở môi trường production khi backend API memberships offline | Ẩn form chuyển khoản, hiển thị thông báo lỗi kết nối có retry | Render bảng giá fallback tĩnh và hiện thông tin STK chuyển khoản thật | Chưa kiểm tra `NODE_ENV === 'production'` để tắt fallback | pending | Tắt API backend -> production hiển thị Error State, không nhận tiền | confirmed-by-code | Khách hàng không thể mua gói khi hệ thống backend bảo trì |
| **SEC-HOTLINE** | P0 | confirmed-by-code | `apps/web/src/` & `apps/api/` | Tìm kiếm số điện thoại cá nhân `0981753082` trong mã nguồn | Không có số điện thoại cá nhân hardcode trong code | Xuất hiện trong test script và một số component UI | Hardcode số điện thoại cá nhân của developer | pending | Grep repo -> 0 kết quả cho số cá nhân | confirmed-by-code | Cần cấu hình hotline trung tâm qua biến môi trường |
| **RB-14** | P0 | confirmed-by-code | `apps/api/src/common/config/assert-env.ts` | Khởi động server production với `SMS_PROVIDER=mock` hoặc thiếu JWT secret | Ứng dụng từ chối khởi động (fail-fast), log lỗi cấu hình rõ ràng | Khởi động âm thầm ở chế độ giả lập nếu thiếu kiểm tra | Chưa kiểm tra đầy đủ các biến môi trường bắt buộc | pending | Khởi động test với env thiếu -> process.exit(1) | confirmed-by-code | Cần điền đầy đủ file `.env` trước khi deploy |
