# Tiến độ dự án

| Hạng mục | Trạng thái | Ghi chú |
|---|---|---|
| 01 - Khởi tạo monorepo | ✅ Xong | Cấu trúc monorepo hoàn chỉnh, `pnpm install` thành công |
| 02 - Database schema & Prisma Client | ✅ Xong | 9 model, `pnpm db:generate` tạo Prisma Client v5.22.0, `pnpm db:migrate` đã chạy migration `init` vào Postgres |
| 03 - Seed dữ liệu nền | ✅ Xong | Địa danh + 2 tài khoản demo + 2 tin `[MẪU]` đã nạp vào DB |
| 04 - API NestJS + Auth OTP | ✅ Xong | `nest build` PASS 100%, Swagger `/docs` hoạt động, auth JWT + OTP mock |
| 05 - Listings CRUD + upload ảnh | ✅ Xong | Upload driver local disk, CRUD 8 endpoints hoạt động |
| 06 - Next.js Frontend | ✅ Xong | `next build` PASS 100%, trang chủ, /mua-ban, /thue, /tin/[slug], /dang-nhap |
| 07 - Tìm kiếm Meilisearch | ⬜ Chưa làm | Đang dùng Prisma filter trực tiếp |
| 08-13 | ⬜ Chưa làm | Xem roadmap CLAUDE.md § 2.8 |

| Audit nghiêm ngặt lần 2 | ✅ Xong | Tìm & sửa 12 lỗi thật — chi tiết đầy đủ trong `TRANG-THAI-TRIEN-KHAI.md`. |
| Audit Claude (01/09/2026) | ✅ Xong | Độc lập tìm và sửa 6 lỗi (#13-#18: JWT fail-fast, lộ tin pending qua ID, API my-listings, addImages 404, refreshToken, 6 route 404). Đã tích hợp. |
| Audit & Hoàn thiện Gemini (02/09/2026) | ✅ Xong | Sửa 6 lỗi bảo mật/runtime (#19-#24); hoàn thiện 8 tính năng/UX chuẩn Mogi.vn (#25-#32). |
| Redesign UI/UX & Audit Đợt 5 (03/09/2026) | ✅ Xong | Redesign phong cách **PropTech Teal** (#0d9488) hiện đại, thoát ly bản sao Mogi; khắc phục 100% WCAG AA (#35); sửa lỗi crash Server Component onClick (#36); thêm 404 & error boundary (#37); chuẩn hóa nhãn `Tin tham khảo` (#38); map enum an toàn (#39); đồng bộ thuật ngữ (#40); module fallback dữ liệu mẫu `demo-data.ts` (#41); xác minh `tsc --noEmit` & `next build` PASS 100% (#42). |
| Tối ưu hoá Hiệu năng & Triệt tiêu Giật Lag (03/09/2026) | ✅ Xong | Khắc phục triệt để đứng hình khi chuyển trang qua bộ skeleton `loading.tsx` (#43); loại bỏ thẻ `<a>` gây hard reload (#44); tối ưu tải ảnh lười và giảm 75% dung lượng ảnh Unsplash (#45); tích hợp React 18 `useTransition` cho bộ lọc phản hồi 76ms (#46); xây dựng `PropertyGallery` đổi ảnh tức thì <4ms (#47); triệt tiêu đúp request trang chi tiết bằng React `cache()` (#48); thêm timeout an toàn 3.5s chống treo SSR và in-memory cache backend (#49); tối ưu CSS hardware acceleration 60-120 FPS (#50). |
| Review Backend & Hệ thống Admin Portal UI Thuần (03/09/2026) | ✅ Xong | Review backend tìm và vá lỗ hổng phân quyền: xây dựng `@Roles()` decorator + `RolesGuard` toàn cục; cập nhật schema (`isBlocked`, `rejectionReason`, report status/resolvedAt); phát triển `AdminModule` (dashboard stats, approve/reject tin, resolve báo cáo vi phạm, quản lý/khóa tài khoản người dùng); xây dựng giao diện Quản trị Portal UI thuần tiếng Việt (`/admin/*`) thay thế hoàn toàn Prisma Studio; bổ sung nút truy cập Quản trị trên Header cho role admin; biên dịch `pnpm build` PASS 100% cả API và Web; Browser Subagent kiểm thử tự động ghi hình thành công. |
| Tách Chuyên Mục Thuê & Đồng bộ Mua BĐS (04/09/2026) | ✅ Xong | Đổi "Bán BĐS" thành "Mua BĐS" đồng bộ; Tách 3 chuyên mục thuê thành 3 trang riêng biệt hoàn toàn (`/thue`, `/cho-thue-tro`, `/cho-thue-mat-bang`); Sửa triệt để lỗi Header active đồng thời cả 3 mục thuê; Xây dựng bộ lọc giá & diện tích đặc thù cho phòng trọ và mặt bằng; Đồng bộ Backend (`categoryGroup`, chuẩn hóa `propertyType`); Cập nhật form Đăng tin `/dang-tin` chọn 4 nhóm danh mục trực quan; Cập nhật trang Admin `/admin/tin-cho-duyet` lọc theo 4 chuyên mục và hiển thị badge tiếng Việt; Build sạch 100% cả API và Web (22 routes). |
| PIVOT CHIẾN LƯỢC: Chuyên biệt hóa 100% "Cho thuê" (05/09/2026) | ✅ Xong | Chuyển dịch toàn diện sang nền tảng broker cho thuê (phòng trọ SV, nhà nguyên căn, căn hộ, studio, mặt bằng); Bỏ 100% mảng mua bán (`TransactionType.sale`, route `/mua-ban` 301 redirect); Mở rộng schema với các trường tiền cọc, hạn hợp đồng, biểu phí điện nước minh bạch, tiện ích phòng, bảng quan hệ `University` & `ListingUniversity`; Xây dựng `EmailModule` (Nodemailer + MOCK) và `GoogleSheetsModule` (Google Sheets API + MOCK) đồng bộ 1 chiều; Xây dựng `UniversitiesModule`; Thay widget tính vay bằng `MoveInCostEstimator` tính chi phí dọn vào tháng đầu; Nâng cấp `/dang-tin` và `/admin/tin-cho-duyet`; Cập nhật `CLAUDE.md`, `README.md`, `TRANG-THAI-TRIEN-KHAI.md`; Build PASS 100% cả API và Web. |
# Tiến độ dự án

| Hạng mục | Trạng thái | Ghi chú |
|---|---|---|
| 01 - Khởi tạo monorepo | ✅ Xong | Cấu trúc monorepo hoàn chỉnh, `pnpm install` thành công |
| 02 - Database schema & Prisma Client | ✅ Xong | 9 model, `pnpm db:generate` tạo Prisma Client v5.22.0, `pnpm db:migrate` đã chạy migration `init` vào Postgres |
| 03 - Seed dữ liệu nền | ✅ Xong | Địa danh + 2 tài khoản demo + 2 tin `[MẪU]` đã nạp vào DB |
| 04 - API NestJS + Auth OTP | ✅ Xong | `nest build` PASS 100%, Swagger `/docs` hoạt động, auth JWT + OTP mock |
| 05 - Listings CRUD + upload ảnh | ✅ Xong | Upload driver local disk, CRUD 8 endpoints hoạt động |
| 06 - Next.js Frontend | ✅ Xong | `next build` PASS 100%, trang chủ, /mua-ban, /thue, /tin/[slug], /dang-nhap |
| 07 - Tìm kiếm Meilisearch | ⬜ Chưa làm | Đang dùng Prisma filter trực tiếp |
| 08-13 | ⬜ Chưa làm | Xem roadmap CLAUDE.md § 2.8 |

| Audit nghiêm ngặt lần 2 | ✅ Xong | Tìm & sửa 12 lỗi thật — chi tiết đầy đủ trong `TRANG-THAI-TRIEN-KHAI.md`. |
| Audit Claude (01/09/2026) | ✅ Xong | Độc lập tìm và sửa 6 lỗi (#13-#18: JWT fail-fast, lộ tin pending qua ID, API my-listings, addImages 404, refreshToken, 6 route 404). Đã tích hợp. |
| Audit & Hoàn thiện Gemini (02/09/2026) | ✅ Xong | Sửa 6 lỗi bảo mật/runtime (#19-#24); hoàn thiện 8 tính năng/UX chuẩn Mogi.vn (#25-#32). |
| Redesign UI/UX & Audit Đợt 5 (03/09/2026) | ✅ Xong | Redesign phong cách **PropTech Teal** (#0d9488) hiện đại, thoát ly bản sao Mogi; khắc phục 100% WCAG AA (#35); sửa lỗi crash Server Component onClick (#36); thêm 404 & error boundary (#37); chuẩn hóa nhãn `Tin tham khảo` (#38); map enum an toàn (#39); đồng bộ thuật ngữ (#40); module fallback dữ liệu mẫu `demo-data.ts` (#41); xác minh `tsc --noEmit` & `next build` PASS 100% (#42). |
| Tối ưu hoá Hiệu năng & Triệt tiêu Giật Lag (03/09/2026) | ✅ Xong | Khắc phục triệt để đứng hình khi chuyển trang qua bộ skeleton `loading.tsx` (#43); loại bỏ thẻ `<a>` gây hard reload (#44); tối ưu tải ảnh lười và giảm 75% dung lượng ảnh Unsplash (#45); tích hợp React 18 `useTransition` cho bộ lọc phản hồi 76ms (#46); xây dựng `PropertyGallery` đổi ảnh tức thì <4ms (#47); triệt tiêu đúp request trang chi tiết bằng React `cache()` (#48); thêm timeout an toàn 3.5s chống treo SSR và in-memory cache backend (#49); tối ưu CSS hardware acceleration 60-120 FPS (#50). |
| Review Backend & Hệ thống Admin Portal UI Thuần (03/09/2026) | ✅ Xong | Review backend tìm và vá lỗ hổng phân quyền: xây dựng `@Roles()` decorator + `RolesGuard` toàn cục; cập nhật schema (`isBlocked`, `rejectionReason`, report status/resolvedAt); phát triển `AdminModule` (dashboard stats, approve/reject tin, resolve báo cáo vi phạm, quản lý/khóa tài khoản người dùng); xây dựng giao diện Quản trị Portal UI thuần tiếng Việt (`/admin/*`) thay thế hoàn toàn Prisma Studio; bổ sung nút truy cập Quản trị trên Header cho role admin; biên dịch `pnpm build` PASS 100% cả API và Web; Browser Subagent kiểm thử tự động ghi hình thành công. |
| Tách Chuyên Mục Thuê & Đồng bộ Mua BĐS (04/09/2026) | ✅ Xong | Đổi "Bán BĐS" thành "Mua BĐS" đồng bộ; Tách 3 chuyên mục thuê thành 3 trang riêng biệt hoàn toàn (`/thue`, `/cho-thue-tro`, `/cho-thue-mat-bang`); Sửa triệt để lỗi Header active đồng thời cả 3 mục thuê; Xây dựng bộ lọc giá & diện tích đặc thù cho phòng trọ và mặt bằng; Đồng bộ Backend (`categoryGroup`, chuẩn hóa `propertyType`); Cập nhật form Đăng tin `/dang-tin` chọn 4 nhóm danh mục trực quan; Cập nhật trang Admin `/admin/tin-cho-duyet` lọc theo 4 chuyên mục và hiển thị badge tiếng Việt; Build sạch 100% cả API và Web (22 routes). |
| PIVOT CHIẾN LƯỢC: Chuyên biệt hóa 100% "Cho thuê" (05/09/2026) | ✅ Xong | Chuyển dịch toàn diện sang nền tảng broker cho thuê (phòng trọ SV, nhà nguyên căn, căn hộ, studio, mặt bằng); Bỏ 100% mảng mua bán (`TransactionType.sale`, route `/mua-ban` 301 redirect); Mở rộng schema với các trường tiền cọc, hạn hợp đồng, biểu phí điện nước minh bạch, tiện ích phòng, bảng quan hệ `University` & `ListingUniversity`; Xây dựng `EmailModule` (Nodemailer + MOCK) và `GoogleSheetsModule` (Google Sheets API + MOCK) đồng bộ 1 chiều; Xây dựng `UniversitiesModule`; Thay widget tính vay bằng `MoveInCostEstimator` tính chi phí dọn vào tháng đầu; Nâng cấp `/dang-tin` và `/admin/tin-cho-duyet`; Cập nhật `CLAUDE.md`, `README.md`, `TRANG-THAI-TRIEN-KHAI.md`; Build PASS 100% cả API và Web. |
| Đánh giá kép & Hoàn thiện Toàn diện (06/09/2026) | ✅ Xong | Đóng 2 vai trò phát hiện và sửa 10 lỗi (#51–#60): Sửa lỗi duyệt/từ chối tin 404/400; đồng bộ enum báo cáo vi phạm; xây dựng `TasksService` tự động quét tin hết hạn & dọn OTP; cấu trúc hóa logging lỗi; tạo 4 trang pháp lý (/dieu-khoan, /chinh-sach, /gioi-thieu, /lien-he); preview thumbnail form đăng tin; lọc tin hết hạn khỏi public search; build 26/26 routes sạch lỗi. |
| Khắc phục lỗi khởi động `pnpm dev` trên Windows Terminal (09/09/2026) | ✅ Xong | Xử lý triệt để 3 nguyên nhân: (1) Cấp quyền ExecutionPolicy cho PowerShell tránh lỗi `PSSecurityException` khi gọi `pnpm.ps1`; (2) Dọn dẹp tiến trình Node treo chiếm cổng 3000 (`EADDRINUSE`) và 4000; (3) Bổ sung `include: ["src/**/*"]` và `exclude: ["node_modules", "dist", "test"]` trong `apps/api/tsconfig.json` giúp Nest CLI compile nhanh, triệt tiêu lỗi `MODULE_NOT_FOUND ./app.module`; loại bỏ cờ `--parallel` đã lỗi thời trong root `package.json`. Xác minh `pnpm dev` chạy sạch 100%, Next.js và NestJS đều phản hồi HTTP 200 OK. |
| Chiến dịch Doanh thu 5 lớp — GĐ 1 & Nền móng GĐ 2 (12/09/2026) | ✅ Xong | Triển khai Lớp 1 (Gói thành viên + Surge Pricing theo mùa) và nền móng Lớp 2 (Trust-as-a-Service badge đã kiểm tra thực tế); Quản trị Mùa cao điểm & Duyệt gói thủ công; Chặn quota tin đăng theo gói; Lên roadmap & điều kiện kích hoạt Giai đoạn 3-5. Build PASS 100% (29/29 routes). |
| Thực thi Audit độc lập — WAVE 0 (12/09/2026) | ✅ Xong | Triệt tiêu backdoor credential admin (P0-01) và thêm bootstrap admin qua secret ngoài repo; Đồng bộ manifest lockfile pass frozen install (P0-05); Build graph tuần tự database -> api -> web (OPS-01); Bổ sung migrate:deploy (OPS-06); Thêm CI GitHub Actions (OPS-07); Rà soát Next.js advisory (OPS-08); Dựng Staging Safety Net chặn gửi SMS/Email thật; Test tự động 11/11 PASS. |
| Thực thi Audit độc lập — WAVE 1 (12/09/2026) | ✅ Xong | Xây dựng Lead entity & LeadsModule API thật, deduplication sha256 chống spam submit, nối ContactBrokerModal (P0-02); Xóa "Tin cậy 100%" & tick xanh vô điều kiện, hiển thị badge xác thực 3 cấp theo CSDL thật (P0-03, FE-07); Tắt fallback demo data ở production tại 4 trang thuê, hiển thị empty/error state trung thực, chặn mutation (/save, /reveal-phone, /report, /leads) trên ID demo (P0-04); Khởi tạo Admin Lead Queue UI (/admin/leads) và Seller Leads UI (/tai-khoan/leads); Build sạch 100% (31/31 routes Next.js, API sạch lỗi); Chạy test suite `test-wave-1.js` 9/9 PASS. |
| Thực thi Audit độc lập — WAVE 2 (12/09/2026) | ✅ Xong | Formatter tài chính chính xác VNĐ integer (P0-08); Predicate public dùng chung active + unexpired + unblocked seller (BE-04, BE-05); Sửa trường cốt lõi hoặc thêm ảnh tự động chuyển về pending và reset huy hiệu xác thực (BE-03); Thêm composite unique constraint PhoneRevealLog chống race condition (BE-09); Sửa lỗi SearchFilterBar areaPresets (FE-01); Forward universitySlug & utilitiesIncluded trên các route thuê (FE-02); Route /thue mặc định hiển thị tất cả loại phòng, không ép căn hộ (FE-03); Tích hợp MoveInCostEstimator & biểu phí minh bạch trên trang chi tiết tin (FE-05); Quản lý tin phân trang & nút đánh dấu đã cho thuê (FE-09, FE-14); Test tự động `test-wave-2.js` 6/6 PASS, `pnpm build` PASS 100% (31 routes). |
| Thực thi Audit độc lập — WAVE 3 (12/09/2026) | ✅ Xong | SMS Adapter thật fail-fast production (P0-06, BE-01); Session Revocation qua tokenVersion trên User model thu hồi phiên tức thì khi logout/đổi mật khẩu/bị khóa (BE-02); Giới hạn upload tối đa 20 ảnh và validate pageSize <= 100 (BE-06); Validate chặt chẽ DTO CreateListingDto (@IsInt price/deposit, @MaxLength title, @Min/@Max lat/lng) (BE-07); Chuẩn hóa client upload trực tiếp Multipart tới backend thay vì presigned-url không tồn tại (FE-06); Test tự động `test-wave-3.js` 5/5 PASS; Build monorepo PASS 100% (31 routes). |
| Thực thi Audit độc lập — WAVE 4 (12/09/2026) | ✅ Xong | Tách quotedAmount khỏi tiền thực thu (pricePaid=0 khi pending) (P0-07, AF-01); Thêm model FinanceLedger & AuditEvent bất biến (AF-10, AF-12); CAS State Machine duyệt/từ chối gói với version check và chống duplicate externalTransactionId (AF-03, BE-13); Renewal policy cộng dồn hạn sử dụng nối tiếp endDate cũ (AF-04); PlanSnapshot bảo toàn quyền lợi bất biến (AF-05); Quota Service đồng bộ active+pending và Admin duyệt tin kiểm tra hạn mức seller (AF-06, AF-07); Dashboard & Duyệt gói hiển thị Sổ cái tài chính, tìm SĐT và hoàn tiền Refund (AF-09, AF-10); Test tự động `test-wave-4.js` 7/7 PASS; Build monorepo PASS 100% (31 routes). |
| Thực thi Audit độc lập — WAVE 5 (12/09/2026) | ✅ Xong | Sanitize chống Formula Injection trên Google Sheets (BE-10); HTML Escaping & loại bỏ dummy email (BE-11); Transactional Outbox Pattern với model OutboxEvent, retry/exponential backoff và Dead Letter Queue DLQ (OPS-03, BE-12); TasksService distributed advisory lock (OPS-04); Kịch bản phục hồi backup & uploads drill (OPS-05); Mobile Sticky Contact Bar & Touch swipe (FE-10); Trợ năng Accessibility ARIA & Escape key (FE-11); SEO thuần cho thuê, Robots chặn staging/admin (FE-12); SLA hỗ trợ 08:00 - 21:30 (FE-13); returnTo an toàn (FE-08); Test tự động `test-wave-5.js` 9/9 PASS; Build monorepo PASS 100%. |
| Thực thi Audit độc lập — WAVE 6 (12/09/2026) | ✅ Xong | Hoàn thành toàn diện mọi chuẩn bị sẵn sàng mở Pilot có kiểm soát: Xây dựng công cụ Synthetic Monitor (`synthetic-monitor.js`), ban hành Pilot Runbook SOP & Incident Log (`docs/ops/PILOT-RUNBOOK-SOP.md`); Định hình phạm vi địa bàn pilot và tiêu chí Go/Hold/Rollback 7 ngày. |
| Thực thi Audit độc lập 3468454 — GATE 0 (12/09/2026) | ✅ Xong | Sửa bộ công cụ xác minh & CI integrity: Viết lại `synthetic-monitor.js` exit 1 khi fail (TST-01); Viết lại `backup-restore-drill.js` sinh dump SQL thật & đo RTO 13.25s (TST-02); Tạo `static-lint-check.js` minh bạch cấu trúc tĩnh (TST-03); Bổ sung lint, xóa `\|\| true` ở audit, thêm production start smoke test vào CI (CI-01..04). |
| Tích hợp báo cáo 19/09 — ĐỢT 0: Chụp hiện trạng (21/09/2026) | ✅ Xong | Tạo đầy đủ 10 file hồ sơ bàn giao §12.1: CURRENT-STATE.md, BUSINESS-MODEL.md, BRAND-AND-TRUST.md, api-inventory.csv, ISSUE-REGISTER.md (hợp nhất F01-F16), PERMISSION-MATRIX.md, DATA-AND-FINANCE-RULES.md, TEST-EVIDENCE.md, RUNBOOK.md, RELEASE-READINESS.md. Cập nhật EXECUTION-STATUS.md. Grep từ cấm: "chính chủ" còn 7 file (Finding BR-01/F15). Đợt 0 hoàn thành xuất sắc, sẵn sàng cho Đợt 1 (Gate A). |

Cập nhật lần cuối: 21/09/2026 (Hoàn thành ĐỢT 0 — Tích hợp báo cáo 19/09, tạo hồ sơ bàn giao §12.1 và hợp nhất F01-F16).

